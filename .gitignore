# dependencies
node_modules/
server/node_modules/

# build output
dist/
dist-admin/
dev-dist/
server/dist/
*.tsbuildinfo

# env / secrets
.env
.env.local
server/.env

# logs
*.log
npm-debug.log*

# runtime-uploaded scavenger-hunt photos + org brand assets (server/lib
# default dirs, incl. BRAND_ASSET_DIR's default server/data/brand-assets)
server/data/

# ffc backups + CenterEdge mock persisted state (bin/ffc, MOCK_STATE_FILE)
data/

# previously-deployed commit, recorded by `ffc deploy` for `ffc rollback`
.ffc-prev-sha

# editor / os
.DS_Store
.vscode/
.idea/

# test output
test-results/
playwright-report/
blob-report/
coverage/

# vision bake-off persistent test data (scripts/lib/vision-compare-store.mjs)
bakeoff-data/
server/bakeoff-data/
__pycache__/
