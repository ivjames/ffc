{
  "name": "ffc-mock-centeredge",
  "version": "0.1.0",
  "description": "Local mock of the CenterEdge POS APIs (F&B ordering + player cards/tickets) so the FFC app's frontend can be built before real API credentials arrive.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "test": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
